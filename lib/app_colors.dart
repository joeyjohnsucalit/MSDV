import 'package:flutter/material.dart';

class AppColors {
  // Restored original/light palette
  static const Color primaryDark = Color(0xFF030357);
  static const Color redAccent = Color(0xFFa80606);
  static const Color background = Color(0xFFF1F4F9);
  static const Color sidebarBg = Color(0xFFF1F1F1);
  static const Color navbarBg = Color(0xFF030357);
  static const Color sidebarHeaderBg = Color(0xFF030357);

  // Aliases to keep current code references working
  static const Color primary = primaryDark;
  static const Color accent = navbarBg;
  static const Color surface = sidebarBg;
  static const Color card = Color(0xFFE6E9F2);
  static const Color inputBg = Color(0xFFFFFFFF);
  static const Color border = Color(0xFFDDDDDD);
  static const Color muted = Color(0xFF6B7280);
  static const Color success = Color(0xFF22C55E);
}
